

# Database Models


#### Datetime usage:
Datetime configuration makes use of `func` from `sqlalchemy` which allows for PostgreSQL database to fill in 
fields of type: 
- `created_at`
- `updated_at`
- `uploaded_at`

